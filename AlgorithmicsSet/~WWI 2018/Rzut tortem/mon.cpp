#include <bits/stdc++.h>

using namespace std;

template<typename T>
T grab()
{
    T x;
    cin >> x;
    return x;
}

int main()
{
    ios::sync_with_stdio(false); cin.tie(nullptr);

    const size_t n = grab<size_t>(), q = grab<size_t>();


}
