#include <bits/stdc++.h>

using namespace std;

int main()
{
    uint32_t n, a, b;
    cin >> n >> a >> b;
    uint32_t d = __gcd(a, b);
    for(uint32_t i = 0; i < n; i++)
    {
        uint32_t q;
        cin >> q; q /= d;

    }
    if(u <= v)
        cout << "Jas";
    else
        cout << "Malgosia";
}
