#include <bits/stdc++.h>

using namespace std;

const size_t MAX = 2e5;

int main()
{
  uint32_t z;
  cin >> z;
  for(uint32_t zi = 0; zi < z; zi++)
  {
    uint32_t n, m;
    cin >> n >> m;

    for(uint32_t i = 0; i < m; i++)
    {
        uint32_t u, v; char c;
        cin >> u >> v;
    }
  }
}
